name: Manage Kaggle Notebooks

on:
  schedule:
    - cron: '0 0,9,18 * * *'
  workflow_dispatch:

jobs:
  run-notebooks:
    runs-on: ubuntu-latest
    env:
      KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
      KAGGLE_KEY: ${{ secrets.KAGGLE_KEY }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Kaggle API credentials
        run: |
          mkdir -p ~/.kaggle
          echo "${{ secrets.KAGGLE_JSON }}" > ~/.kaggle/kaggle.json
          chmod 600 ~/.kaggle/kaggle.json

      - name: Install Kaggle CLI
        run: pip install kaggle

      - name: Prepare Notebook With Timestamp
        run: |
          TIMESTAMP=$(date +'%Y%m%d%H%M%S')
          NOTEBOOK_DIR="notebook_${TIMESTAMP}"
          NOTEBOOK_TITLE="Notebook_${TIMESTAMP}"  # 使用带时间戳的名称
          mkdir -p ${NOTEBOOK_DIR}
          cp script/main.py ${NOTEBOOK_DIR}/main.py
          cp script/kernel-metadata.json ${NOTEBOOK_DIR}/kernel-metadata.json
          # 在 kernel-metadata.json 文件中更新 title 字段，添加时间戳
          sed -i "s/\"title\": \".*\"/\"title\": \"${NOTEBOOK_TITLE}\"/" ${NOTEBOOK_DIR}/kernel-metadata.json

      - name: Debug - Print Variables
        run: |
          echo "Notebook Directory: ${NOTEBOOK_DIR}"
          ls ${NOTEBOOK_DIR}

      - name: Push and Run Notebook
        run: |
          kaggle kernels push -p "${NOTEBOOK_DIR}"
          echo "Notebook ${NOTEBOOK_TITLE} submitted and running."

      - name: Clean Up
        run: |
          rm -rf "${NOTEBOOK_DIR}"
          echo "Temporary notebook folder deleted."
